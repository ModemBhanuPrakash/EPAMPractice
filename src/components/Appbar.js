import { AppBar, MenuItem, Toolbar } from '@mui/material'
import React from 'react'

const Appbar = () => {
  return (
    <AppBar>
        <Toolbar>
            <MenuItem>Bhanu</MenuItem>
        </Toolbar>
    </AppBar>
  )
  
}

export default Appbar
